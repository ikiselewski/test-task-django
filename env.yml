name: test-task-django
channels:
  - conda-forge
dependencies:
  - python=3.11
  - django=5.1
  - mamba  # or use conda
  # Additional for VCF parsing - optional, can use csv if simple
  - pandas  # for potential VCF parsing
